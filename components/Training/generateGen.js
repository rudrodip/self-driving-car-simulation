import generateGenomes from "./generateGenomes";

const generateGen = (populationCount, genomeCount, rayCount) => {
  let population = []
  for (let i = 0; i < population; i++){
    let genomes = generateGenomes()
  }
}